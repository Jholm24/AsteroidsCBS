# AsteroidsCBS
To start the game run either of these commands in the console. Only use PS script if on windows.
**mvn clean install** or use  **build-windows-ps1** script if windows locks folder

then run **mvn exec:exec** and play the game
